# Screenshots

Place reviewed dashboard captures in this directory. Runtime screenshots created by the application use the pattern `dashboard_YYYYMMDD_HHMMSS.png` and are ignored by Git by default.

Use captions that identify the test condition, serial connection state, and whether the image represents live hardware or sample data.
